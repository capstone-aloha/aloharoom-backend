
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://capstone.ci26bpttokr7.ap-northeast-2.rds.amazonaws.com:3306/sys?serverTimezone=Asia/Seoul
    username: admin
    password: dEs0tZHqOxYYuGgGmMRR
  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  output:
    ansi:
      enabled: always
logging.level:
  org.hibernate.SQL: debug